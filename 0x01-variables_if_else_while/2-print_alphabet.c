#include <stdio.h>
/**
 * main - Entry point
 * Description: random number in n +ve/-ve 
 * Return: 0 on Success
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		putchar(c);
		c++;
	}
	putchar('\n');

	return (0);
}
